FUNCTION main() RETURNS INTEGER
    // ignore me
    CONSTANT x = 1

    IF x = 1 THEN
        CONSTANT y = 1
    ELSE
        IF 2 THEN
            CONSTANT z = 2
            RETURN 1
        ENDIF
    ENDIF

    RETURN 0
ENDFUNCTION
